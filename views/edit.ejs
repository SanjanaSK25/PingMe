<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit the Msg</title>
</head>
<body>
    <h1>You are about to edit your chat : <%= chat._id %> </h1>
    <p>This chat was sent from <%= chat.from %> to <%= chat.to %></p>
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="msg" rows="5" cols="10"><%= chat.msg %></textarea>
        <button>Edit</button>
    </form>
</body>
</html>